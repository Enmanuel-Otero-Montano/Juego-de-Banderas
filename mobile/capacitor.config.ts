import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.enmanuelotero.atlasflags',
  appName: 'Banderas, Países y Regiones',
  webDir: 'dist',
  backgroundColor: '#F5F7F2',
  android: {
    backgroundColor: '#F5F7F2',
  },
};

export default config;
